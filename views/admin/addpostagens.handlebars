<div class="card">
    <div class="card-body">
            <h1>Nova postagem</h1>
        <form action="">
            <label for="titulo">Título</label>
            <input type="text" name="titulo" id="" class="form-control">
            <label for="slug">Slug</label>
            <input type="text" name="slug" class="form-control" id="">
            <label for="descricao">Descricao: </label>
            <input type="text" name="descricao" class="form-control" id="">
            <label for="conteudo">Conteudo</label>
            <textarea name="conteudo" class="form-control" id="" cols="30" rows="10"></textarea>
            <label for="categoria">Categoria: </label>
            <select name="categoria" id="" class="custom-select">
                {{#each categorias}}
                    <option value="{{_id}}">{{nome}}</option>
                {{else}}
                    <option value="">Nenhuma categoria registrada</option>
                {{/each}}
            </select>
            <a href=""><button  class="btn btn-sucess mt-4" type="submit">Criar postagem</button></a>
        </form>
    </div>
</div>